# Postgres. Em produção use a string de conexão do seu provedor
# (Neon, Supabase, Railway, Render...).
DATABASE_URL="postgresql://USUARIO:SENHA@HOST:5432/controlealunos?schema=public"

# Segredo que assina o cookie de sessão. Gere com: openssl rand -base64 32
AUTH_SECRET="gere-com-openssl-rand-base64-32"
